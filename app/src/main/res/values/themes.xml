<resources>
    <!--
      Minimal platform theme. All real styling happens in Compose
      (MaterialExpressiveTheme), so this only needs to be an edge-to-edge,
      action-bar-free shell with the right window background.
    -->
    <style name="Theme.Remiit" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>

    <!-- Applied at launch, handed off to Theme.Remiit once Compose draws. -->
    <style name="Theme.Remiit.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Remiit</item>
    </style>

    <!--
      The reminder overlay. Transparent and floating so a full-screen banner can
      animate in over whatever is on screen instead of hard-cutting to an opaque
      activity.
    -->
    <style name="Theme.Remiit.Overlay" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowShowWallpaper">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
